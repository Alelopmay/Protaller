export interface Company {
    latitude: any;
    longitude: any;
    message: Company;
    id: number;
    name: string;
    password: string;
    address: string;
    location: { lat: number; lng: number } | null;
    schedule: string;
}
